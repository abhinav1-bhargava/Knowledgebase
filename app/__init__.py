"""Streamlit UI layer.

Two separate apps share the same backend (query engine + storage):

    consumer     - Chat interface for PMs asking onboarding questions
    contributor  - Curation interface for pod contributors to answer gaps,
                   manage verified answers, and view query analytics
"""
